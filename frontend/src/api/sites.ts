import Curd from '@/api/curd'
import http from '@/lib/http'
import {AxiosRequestConfig} from "axios/index";

class Site extends Curd {
    enable(name: string, config:AxiosRequestConfig) {

    }
}


